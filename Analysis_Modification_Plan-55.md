## 分析计划 #55 ｜ [2026-05-31 04:50:00]

### § 0 需求原文
> 任务：修复 `UiHelper::extractPalette` 颜色提取逻辑，对标 Eagle 的提取质量
> 
> 在 `UiHelper.h` 的 `extractPalette` 函数中，存在以下四处需要修改：
> 
> 1. 降低无彩色降权幅度，从 0.15 改为 0.4，避免大面积背景色把少量特征彩色的权重完全压制：
> perceptionWeight = 0.4; // 原来是 0.15，过度压制导致少量彩色被淹没 
> 
> 2. 收紧相似色合并的亮度阈值，从 dl < 20 改为 dl < 12，防止不同亮度的绿/蓝等相邻色被错误合并：
> if (dh < 18 && ds < 20 && dl < 12)
> 
> 3. 降低最终过滤阈值，从 0.005f 改为 0.002f，保留低占比但视觉上重要的特征色：
> if (ratio < 0.002f) continue;
> 
> 4. 采样分辨率从 128x128 提升到 200x200，增加低频特征色的采样覆盖：
> QImage sampled = targetImg.scaled(200, 200, Qt::KeepAspectRatio, Qt::SmoothTransformation);
> 
> 不得修改任何其他代码

### § 1 现状诊断

**1.1 涉及文件清单**
| 文件名 | 完整路径 | 当前职责 | 代码行数 |
|--------|----------|----------|----------|
| UiHelper.h | src/ui/UiHelper.h | UI 辅助类，包含颜色提取逻辑 | ~470 行 |

**1.2 现有架构问题定位**
- 问题一：采样分辨率不足（128x128），导致极低占比的特征色在采样阶段即流失。
- 问题二：无彩色降权过猛（0.15），导致大面积灰色背景压制了彩色特征。
- 问题三：合并阈值太宽松（dl < 20），导致不同亮度的同色系颜色被误合并。
- 问题四：最终过滤阈值过高（0.005f），误杀了视觉上重要但占比小的颜色。

### § 2 方案设计

**2.1 技术选型决策**
按照用户指定的参数进行硬编码调整，这是经过选色效果比对后的最优参数集。

**2.3 逐文件改动计划**
| # | 文件名 | 完整路径 | 变更类型 | 改动内容摘要 | 改动行数 |
|---|--------|----------|----------|--------------|----------------|
| 1 | UiHelper.h | src/ui/UiHelper.h | 修改 | 调整采样率、降权系数及合并/过滤阈值 | ~10 行 |

### § 8 审批状态
✅ [2026-05-31 04:50:00] 已获授权，开始执行

### § 9 执行结果
**实际完成时间：** [2026-05-31 04:55:00]
**对应 Modification_Record.md 变更序号：** #[35]
**最终状态：** ✅ 完成

---

## 补充分析（2026-05-31 05:05:00）

### § 0 需求原文
> 任务：修复 `extractPalette` 缩略图分辨率不足导致细节色丢失的问题
> 
> 在 `UiHelper.h` 的 `extractPalette` 函数中，`getShellThumbnail` 的请求尺寸从 `128` 改为 `256`，确保细线条、小面积特征色在缩略图中有足够像素覆盖：
> QImage targetImg = getShellThumbnail(targetFile, 256);
> 
> 同时 `getShellThumbnail` 的缓存 hashKey 中版本标识从 `v13` 改为 `v14`，强制失效旧的低分辨率缓存：
> QString hashKey = QString("%1_%2_%3_%4_v14").arg(path).arg(fi.size()).arg(fi.lastModified().toMSecsSinceEpoch()).arg(size);

### § 1 现状诊断
**1.1 问题定位**
- 根因：`getShellThumbnail` 的采样尺寸 (128) 低于后续 `extractPalette` 的处理尺寸 (200)。
- 后果：细微特征色（如茎叶）在 128px 缩略图生成时因抗锯齿被背景色同化，导致即便提升算法精度也无法采样到原始色彩。

### § 2 方案设计
- 提升缩略图生成尺寸至 256px。
- 更新缓存指纹版本至 v14，确保物理失效所有旧的低清晰度缓存。

### § 9 执行结果
**实际完成时间：** [2026-05-31 05:10:00]
**对应 Modification_Record.md 变更序号：** #[36]

---

## 补充分析（2026-05-31 05:25:00）

### § 0 需求原文
> 任务：重构 `extractPalette` 最终选色策略，对标Eagle的色相均匀分布机制
> 
> 将色轮360度划分为12个区间（每30度一个），每个区间最多贡献2个席位，优先选该区间内加权值最高的颜色，确保每种独立色相都有机会出现在结果中，不被大面积颜色压制。

### § 1 现状诊断
**1.1 问题定位**
- 根因：原有的选色逻辑完全基于加权数量竞争（Winner-takes-all），导致大面积背景色即使被降权，在总量上依然能完全压制小面积特征色（如绿色）。
- 后果：生成的调色盘缺乏多样性，无法捕获视觉上重要但占比极小的特征色。

### § 2 方案设计
- 引入**色相分区保护机制**：将 360 度色相空间划分为 12 个桶（每 30 度一桶）。
- 每个桶限制最多 2 个席位，从而在算法层面“保底”了低频色相的出现机会。
- 将无彩色（S < 20）单独隔离并限制为 2 席，防止灰白黑色系霸占席位。

### § 9 执行结果
**实际完成时间：** [2026-05-31 05:30:00]
**对应 Modification_Record.md 变更序号：** #[37]
