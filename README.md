# 备份备注

**备份时间**：2026-04-13 11:05:16  
**备份目录**：Buk_20260413_110516  

---

版本1

此提交完整实现了磁盘扫描逻辑的“去中心化”：

- **冷启动极致优化**：取消了 `startSystem` 阶段的物理扫描逻辑，启动界面仅显示“载入元数据缓存”（内存数据库加载），实现了真正的按需启动。
- **扫描交互升级**：
    - 在标题栏新增扫描入口按钮。
    - 弹出 `ScanDialog` 允许用户自由勾选需要同步的分区。
    - 实时反馈扫描路径及 GLOB 处理进度，不阻塞主线程。
- **鲁棒性修复**：
    - 删除了遗留的 `AmMetaJson.cpp` 编译报错源。
    - 强制刷新构建配置解决 MSVC 链接符号未定义问题。
