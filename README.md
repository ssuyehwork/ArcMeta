# 备份备注

**备份时间**：2026-06-11 19:50:27  
**备份目录**：Buk_20260611_195026  

---

为了查明“5万条数据扫描闪退”的根本原因，本提交实现了全套“取证”机制：
1. **WER 自动化配置**：提供 `configure_crash_dumps.bat`，一键开启 ArcMeta.exe 的完整转储 (Full Dump) 生成。
2. **日志强力落盘**：修改 `src/main.cpp`，在日志处理程序中增加 `flush()` 调用，确保崩溃前的最后一行线索不会因缓冲区丢失。
3. **自动化资源监控**：在 `src/ui/MainWindow` 中集成定时监控逻辑，每 5 秒将进程的 GDI 对象、USER 句柄及私有内存占用 (Private Bytes) 自动写入日志，以便验证是否存在句柄泄漏或内存溢出。
4. **编译与文档对齐**：更新 `CMakeLists.txt` 链接 `psapi` 库，并在 `AGENTS.md` 中记录了此项诊断规范。

这些改动为后续“对症下药”提供了坚实的证据基础，避免盲目重构。
