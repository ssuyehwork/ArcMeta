我已加载 C++/Qt 终极宪法，只说中文，绝不越权乱改，已开启强制握手模式等待您的指令。

针对您发现的“空白”问题，我认为必须从**“数据加载策略”**和**“UI 响应模式”**两个维度进行手术。C++ 版目前在模仿 Rust 的“形”（UI 样式），但没学到“神”（高性能索引架构）。

### 阶段 1：方案深度分析与方案建议

我建议采取以下三个核心步骤来彻底消灭空白期，达到 Rust 版的瞬时响应效果：

#### 1. 核心手术：从“全量载入”切换为“内存映射 (mmap)”
*   **现状**：`MftReader::loadFromCache` 正在老老实实地从磁盘读取字节并解析成一个个对象。
*   **改进**：利用 Windows API（`CreateFileMapping` / `MapViewOfFile`）直接将缓存文件映射到进程地址空间。
    *   **预期结果**：载入时间从几秒缩短至**几毫秒**。由于没有“搬运数据”的过程，窗口打开时数据已经在内存地址里等着渲染了。

#### 2. UI 策略：引入“流式首屏渲染”
*   **现状**：必须等整个缓存解析完，才通过 `onTriggerSearch` 把数据灌入 `ScanTableModel`。
*   **改进**：
    *   在 `MftReader` 开始映射后，立即给 UI 发送一个“已就绪”信号，即使后台还在进行 USN 校验。
    *   让 `ScanTableModel` 初始显示的 200 条数据来自于映射文件的头部，不再等待全量搜索。
    *   **预期结果**：用户点开界面，**第一帧**就能看到文件列表。

#### 3. 架构同步：采用 SoA (Structure of Arrays) 存储架构
*   **现状**：C++ 可能还在用 `struct Record { string name; uint64_t size; ... }` 这种存储方式（AoS）。
*   **改进**：模仿 Rust 版，将名字、大小、路径分别存储在独立的连续数组中。
    *   **优势**：这种结构对缓存极其友好，且在进行内存映射时，可以直接把整个文件按列对齐读取。

---

### 建议的操作方案

为了确保一次性改好，我需要先查看底层 `MftReader` 的实现代码，确认缓存文件的格式。

**我计划执行的下一步动作（需您授权）：**
1.  读取 `MftReader.h` 和 `MftReader.cpp`。
2.  设计一套基于 Windows `mmap` 的零拷贝载入逻辑。
3.  修改 `ScanDialog` 的初始化顺序，让其在映射完成后立即显示首屏数据。

---

🛑 [系统锁定]：方案已就绪，等待授权。请回复【授权修改】，我才会开始触碰代码。

🧱 [宪法合规自查]
- 本次是否触发了阶段 1→2→3 状态机？[是]
- 本次输出是否包含省略号/占位符？[无]
- 本次是否存在未授权的范围蔓延？[无]
- 本次头文件同步是否已自查？[已分析计划]
- 本次复用代码是否已适配自查？[待适配]
- 以上全部合规，本次提交合法。