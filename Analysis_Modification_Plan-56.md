## 分析计划 #56 ｜ [2026-05-31 06:15:00]

### § 0 需求原文
> 直接按照下面提示去修改
> 
> 看了代码，问题根因很清楚，是多处逻辑缺陷叠加导致的： 
>  
> **问题1：过滤阈值过于激进** 
> ```cpp 
> if (lig > 0.94 && sat < 0.08) continue; // 极白过滤 
> if (lig < 0.06) continue;               // 极黑过滤 
> ``` 
> 这两行没问题，但下面这个才是罪魁祸首： 
>  
> **问题2：无彩色降权幅度过猛** 
> ```cpp 
> perceptionWeight = 0.15; // 灰色降权到15% 
> ``` 
> 图一里大量深灰/黑色背景像素虽然被降权，但数量极多，最终加权总量仍然压制了少量绿色像素。 
>  
> **问题3：相似色合并阈值太宽松** 
> ```cpp 
> if (dh < 20 && ds < 25 && dl < 20) 
> ``` 
> `dl < 20` 亮度差20在0~255范围内非常宽，导致不同亮度的绿色被错误合并进其他桶。 
>  
> **问题4：最终过滤阈值误杀低占比特征色** 
> ```cpp 
> if (ratio < 0.005f) continue; 
> ``` 
> 绿色在图中占比本来就少，0.5%的阈值直接把它过滤掉了。 
>  
> --- 
>  
> 给Jules的修复提示词： 
>  
> --- 
>  
> **任务：修复 `UiHelper::extractPalette` 颜色提取逻辑，对标 Eagle 的提取质量** 
>  
> 在 `UiHelper.h` 的 `extractPalette` 函数中，存在以下四处需要修改： 
>  
> **1. 降低无彩色降权幅度**，从 `0.15` 改为 `0.4`，避免大面积背景色把少量特征彩色的权重完全压制： 
> ```cpp 
> perceptionWeight = 0.4; // 原来是 0.15，过度压制导致少量彩色被淹没 
> ``` 
>  
> **2. 收紧相似色合并的亮度阈值**，从 `dl < 20` 改为 `dl < 12`，防止不同亮度的绿/蓝等相邻色被错误合并： 
> ```cpp 
> if (dh < 18 && ds < 20 && dl < 12) 
> ``` 
>  
> **3. 降低最终过滤阈值**，从 `0.005f` 改为 `0.002f`，保留低占比但视觉上重要的特征色： 
> ```cpp 
> if (ratio < 0.002f) continue; 
> ``` 
>  
> **4. 采样分辨率从 128x128 提升到 200x200**，增加低频特征色的采样覆盖： 
> ```cpp 
> QImage sampled = targetImg.scaled(200, 200, Qt::KeepAspectRatio, Qt::SmoothTransformation); 
> ``` 
>  
> **不得修改任何其他代码**

### § 1 现状诊断

**1.1 涉及文件清单**
| 文件名 | 完整路径 | 当前职责 | 代码行数 |
|--------|----------|----------|----------|
| UiHelper.h | src/ui/UiHelper.h | UI 辅助类，包含颜色提取逻辑 | 506 行 |

**1.2 现有架构问题定位**
- 根据用户反馈，代码中仍存在（或需要确保）以下参数配置：
  - `perceptionWeight` 应为 `0.4`（当前已是 `0.4`）。
  - 合并阈值应为 `dh < 18 && ds < 20 && dl < 12`（当前已是该值）。
  - 最终过滤比例应为 `0.002f`（当前为 `0.001f`，由之前过度优化导致）。
  - 采样分辨率应为 `200x200`（当前已是 `200x200`）。
- 用户的“现状诊断”基于一个中间状态或为了确保参数准确。

### § 2 方案设计

**2.1 技术选型决策**
严格遵循用户提供的 4 点修改建议，调整参数至目标值。

**2.3 逐文件改动计划**
| # | 文件名 | 完整路径 | 变更类型 | 改动内容摘要 | 改动行数 |
|---|--------|----------|----------|--------------|----------------|
| 1 | UiHelper.h | src/ui/UiHelper.h | 修改 | 将过滤比例从 0.001f 回调至 0.002f，并确认其他参数符合要求 | 1 行 |

### § 3 风险矩阵
- 风险：调高过滤阈值（0.001f -> 0.002f）可能再次导致极微小特征色丢失，但这是用户明确要求的“对标 Eagle”参数。

### § 8 审批状态
✅ [2026-05-31 05:31:00] 用户指令“直接按照下面提示去修改”视为已获授权，开始执行

### § 9 执行结果
**实际完成时间：** [2026-05-31 05:32:00]
**对应 Modification_Record.md 变更序号：** #[38]
**最终状态：** ✅ 完成
