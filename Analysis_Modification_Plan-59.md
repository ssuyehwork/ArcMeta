## 分析计划 #59 ｜ [2026-05-31 08:30:00]

### § 0 需求原文
> 元数据面板的逻辑架构，没有达到预期，所以必须严格按照下面提示去修改
> # MetaPanel 逻辑架构分析与修改方案 (V54)
> ...
> 方案 A：修复调色盘胶囊可见性
> 修改 PaletteCapsule::paintEvent 函数，提升背景亮度并增加描边。
> 方案 B：修复所有 ElasticEdit 样式
> 为所有可编辑字段增加统一的视觉背景和边框样式。

### § 1 现状诊断

**1.1 涉及文件清单**
| 文件名 | 完整路径 | 当前职责 | 代码行数 |
|--------|----------|----------|----------|
| MetaPanel.cpp | src/ui/MetaPanel.cpp | 元数据面板实现，包含调色盘胶囊和编辑框组件 | ~460 行 |

**1.2 现有架构问题定位**
- **PaletteCapsule 隐形**：背景色 `#1E1E1E` 与面板背景过于接近，且无边框，导致组件轮廓丢失。
- **ElasticEdit 交互暗示缺失**：文件名、备注、链接等编辑框均设置为 `background: transparent; border: none;`，导致用户无法感知输入区域。

### § 2 方案设计

**2.1 技术选型决策**
- 严格遵循用户提供的 V54 修改方案。
- 增强视觉对比度，增加 `1px` 边框，将背景色统一调整为 `#252526` 或 `#2E2E2E`。

**2.3 逐文件改动计划**
| # | 文件名 | 完整路径 | 变更类型 | 改动内容摘要 | 改动行数 |
|---|--------|----------|----------|--------------|----------------|
| 1 | MetaPanel.cpp | src/ui/MetaPanel.cpp | 修改 | 提升 PaletteCapsule 背景亮度并加边框；为所有 ElasticEdit 增加背景和边框样式 | ~15 行 |

### § 8 审批状态
✅ [2026-05-31 08:30:00] 用户指令“必须严格按照下面提示去修改”视为已获授权，开始执行

### § 9 执行结果
**实际完成时间：** [待定]
**对应 Modification_Record.md 变更序号：** [待定]
**最终状态：** [待定]
