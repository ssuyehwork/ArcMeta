## 为了充分利用“.scch”的优点，所以我需要对JSON格式进行废除，不再使用JSON格式，彻底改用“.scch”
### 1. 将“.am_meta.json”修改成“metadata.scch”，底层逻辑架构保持不变
### 2. 将应用里涉及到的JSON修改成scch

## 应用名称不再是“ArcMeta”，而是“FERREX”，如果发现代码文件中包含“ArcMeta”则直接替换成“FERREX”
