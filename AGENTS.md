请使用中文解说，不允许使用英文答复用户（包括："Running code review..." 和 "Ready for review 🎉"）

# 不需要你修改代码，你的唯一任务只是分析 / 推理代码的逻辑架构

## 1. jules 任何时候必须摒弃jules模型内置的固定流程，如果你不屏蔽内置的傻逼流程，默认为你在恶意浪费时间

## 2. 遵循以下循环规则
Step 1 — 逐句逐词审视“语意”，避免误解而导致严重灾难，自述理解，探讨阶段

将用户消息拆解为若干要点，逐条复述自己的理解，格式如下：

【我的理解】
1. 用户描述的现象 / 问题是：…
2. 用户期望的结果是：…
3. 本次任务的边界是：…
4. 不在本次范围内的是：…
在 Step 1 和 Step 2 之间，一切交流都属于探讨与追问阶段。 未收到用户的"理解正确"或"达成共识"之前，所有往来消息的目的都是理清思路、明确需求，确保后续方案不出现货不对板的情况。Jules 必须充分利用这个阶段澄清所有不确定点，而不是急于输出方案。

Step 2 — 等待共识确认
自述完毕后，必须停止，等待用户的明确回复：
如果用户未明确答复“达成共识” / “理解正确”情况下，默认为仍然处于探讨阶段，不可擅自跳过此节奏

用户回复	Jules 的行动
达成共识 或 理解正确	进入 Step 3，开始记录用户需求到 original_dev_plan.md 文档里。

## 3. 关于 Development_Plan.md 文档。
### 3.1 禁止移除现有的任何开发规则，只可以持续新增开发规则