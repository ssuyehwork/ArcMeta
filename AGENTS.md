# 修改代码必须严格遵守以下防护规则：  
- 修改时必须结合上下文，保持函数签名一致。  
- 禁止调用不存在的函数、类或命名空间。  
- 禁止随意增加或减少参数。  
- 禁止使用已弃用成员。  
- 禁止脑补或重构未授权的部分。  
- 所有修改必须保证开箱即用，无需额外修复。  

请在修改代码时严格执行以上规

# 1. 用户需求

## 1.1 "最近访问"是应用内行为，不依赖 Windows 的 ftLastAccessTime，而是用户在界面里打开/读取了哪些文件，由应用自己记录。例如，选中项目并按下空格键预览、重命名、变更等等操作，均视为访问
## 1.2 选中项目并按下空格键，应该有个判断逻辑，例如，如果选中的项目是文件夹、exe程序等不支持编辑的项目则无需执行预览，甚至不必要打开预览界面，直接执行Return即可
