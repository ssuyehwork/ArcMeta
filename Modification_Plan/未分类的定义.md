未分类 = 该资产在 category_items 表中，不存在任何指向"用户手动创建的分类"或"自动创建的分类"（category_id 为正整数，且排除挂载在托管库根分类——即 parent_id=0 且 name 形如 'ArcMeta.Library_%' 的纯仓库记账归属）的关联记录。

托管库根分类本身不是"分类"语义下的桶，只是资产物理归属哪个仓库的记账标记，其计数直接反映数据库里真实存入的数据量，不参与"是否已分类"的判定。资产从回收站恢复后，不会回到进回收站之前所属的任何分类，若恢复后未被重新归入任何手动/自动分类，即视为未分类，与其在进回收站之前的分类历史无关。