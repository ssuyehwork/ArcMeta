1. 关于“.scch”
“.scch”和“mainwindow”窗口没有任何关系
“.scch”仅可应用在“ScanDialog.cpp”，只有在点击“扫描”按钮后加载“.scch”并在打开“ScanDialog.cpp”界面后才可以使用，当关闭“ScanDialog.cpp”界面后同时卸载“.scch”。所以“.scch”缓存文件和主界面没有任何关系，这是为了不占用内存

2. “ScanDialog.cpp”界面必须支持.svg 图片，可以直接参考/调用mainwindow 内容容器显示的svg方式

3. MainWindow 滚动条样式偏好（QSS 参数）
为了保持 UI 的一致性与“物理切割感”，后续所有界面添加滚动条时必须遵循以下样式：
- **背景 (Background)**: `transparent` (透明)
- **边框 (Border)**: `none`
- **宽度/高度 (Width/Height)**: 垂直方向 `width: 4px`, 水平方向 `height: 4px`
- **外边距 (Margin)**: `0px`
- **滑块颜色 (Handle Background)**: 默认 `#333333`, 悬停(hover) `#444444`
- **滑块圆角 (Handle Border-radius)**: `2px`
- **最小长度 (Min-length)**: 垂直方向 `min-height: 20px`, 水平方向 `min-width: 20px`
- **按钮 (Add/Sub-line)**: 必须设置为 `height: 0px` 或 `width: 0px` (彻底隐藏箭头按钮)
- **页面区域 (Add/Sub-page)**: `background: none`

---
**2026-05-21 需求记录：**
1. **数据库分析**：分析当前版本DB类数据库是否采用唯一FRN（File Reference Number）方式，以避免同一文件重复创建数据。
2. **列表计数排查**：分析并修正了状态栏与分类面板计数不一致的问题。现在状态栏能够真实统计并显示可见的文件与文件夹数量。
3. **取消加载限制**：废除数据库查询中对 1000 条数据的 LIMIT 1000 限制。
4. **ScanDialog SVG支持**：ScanDialog.cpp 界面支持显示 .svg 类型图片。
5. **图标系统架构对齐（方案 B）**：按用户明确要求，图标的降级/非缩略图显示机制必须统一为 Windows 系统原生关联图标风格。

---
**2026-06-05 需求记录 (UI 核心规范 - 终极锁定)：**
1. **缩放红线锁定**：物理强制 ContentPanel 卡片缩放最小值为 **96 像素**。
2. **视图自动切换**：
   - 当在 **96 像素** 继续向下缩小时，界面必须自动切换至列表模式。
   - 当在列表模式向上放大且行高超过 **96 像素** 时，界面必须自动切换回网格模式，并以 **96 像素** 作为起始缩放值。
3. **列表视图极致规范**：
   - **高亮视觉**：必须改为**直角、全行贯穿式填充**（`setSelectionBehavior(QAbstractItemView::SelectRows)`），填满整行区域，禁绝圆角与缝隙。
   - **架构对齐**：采用 7 列架构（名称、状态、星级、颜色标记、类型、大小、修改时间）。
   - **选中反馈**：选中时，即使评分为 0，也必须显示“禁止图标”和“空心星级”占位，确保交互反馈的一致性。
4. **持久化要求**：
   - 视图模式 (ViewMode) 必须物理记录并持久化加载。
   - 列表 Header 状态（包含列宽、排序）必须在模型重置后正确恢复。
5. **动态比例星级**：
   - 仅作为低行高兼容方案保留。
   - 当行高/缩放 `< 60` 时，自动将星级图标缩小 20%。
   - 当行高/缩放 `< 40` 时，自动隐藏评分栏。

---
**2026-05-20 需求记录：**
1. **品牌更名**：MainWindow 标题栏显示的名称由 "ArcMeta" 修改为 "FERREX"。
2. **视觉对齐**：内容容器卡片下方的“禁止”图标与“星级”图标视觉大小一致处理。
3. **交互优化**：移除“再次点击选中项触发编辑”的机制，仅保留快捷键或专用菜单触发。

**2024-10-10 需求记录：**
1. **图标系统重构**：物理删除了 `resources/icon` 目录下所有的 SVG 文件。
2. **资源引用优化**：全面排查并移除了代码中对 `:/icons/*.svg` 的路径引用。
3. **动态加载机制**：在 `UiHelper` 中实现了 `getSvgDataUrl` 函数。

**2024-10-21 需求记录 (Eagle 布局增强)：**
1. **布局核心**：实现类似 Eagle 的 Justified Layout。`JustifiedView.cpp` 的 `doLayout` 每行额外增加 36px 空间显示文件名。
2. **卡片视觉**：`ThumbnailDelegate.cpp` 实现 6px 圆角，缩略图 `KeepAspectRatioByExpanding` 铺满。

// ===================|===================

# 🚨 核心警示 🚨
**禁止任何基于“脑补”的功能破坏。所有 UI 数值的变更、逻辑架构的修改必须以用户的明确指令为准。**

1. 为了降低后期的维护成本，将曾经的错误记录到这里：**严禁将缩放红线设定为 56 像素，严禁破坏列表模式的全行选中特性。**

2. 右键菜单的圆角设计为 8 像素

3. 悬浮面板 圆角设计为 6 像素

4. 悬停高亮 圆角设计为为 4 像素

---
**2026-05-23 记录：**
1. **图片倒置修复**：通过 GetDIBits 并强制指定 top-down 方向修复 QImage::fromHBITMAP 倒置问题。
2. **UI偏好**：ContentPanel 中的星级部分不应位于卡片内部，应显示在卡片与文件名称的中间位置。

---
**2026-06-16 记录：**
1. **工业级 UI 交互架构升级与逻辑加固**：
   - **8 列架构锁定**：重构列表视图为 8 列架构（名称、状态、星级、颜色标记、标签、类型、大小、修改日期），并填充物理数据。
   - **宽度守恒与弹性布局**：实现 `QHeaderView` 交互式自平衡布局。废除末端强制 Stretch，改由“名称”列（索引 0）作为主弹性列。为“修改日期”列设置 150px 物理最小红线（setMinimumSectionSize），同时保持所有列的 Interactive 模式，支持用户自由调整宽度的同时确保核心信息不被遮挡。引入 `m_isLoading` 状态锁防止数据加载时的布局抖动覆盖用户保存的列宽状态。
   - **交互降噪与闭环**：优化星级列显示逻辑，仅在选中或评分 > 0 时显示交互图标。支持列表模式下点击“禁止图标”一键清空评分（统一由 setData 触发，消除双写冲突）。
   - **Hitbox 对齐**：修复 JustifiedView 双击文本区域 Hitbox 漂移问题，同步 doLayout 计算偏移量。
   - **多选锚点固定**：修正 Shift+点击逻辑，取消在 Shift 选择过程中对 currentIndex 的非必要更新，确保连续多选锚点不丢失。
2. **布局物理切割感增强**：优化 MainWindow 中 QSplitter 的样式，确保 5px 缝隙清晰可见，并添加悬停反馈。
3. **多选逻辑修正**：在网格模式下重构了 Shift+左键多选逻辑，确保选择范围遵循索引顺序（从左向右到下一行），符合常规用户习惯。

---
**2026-06-18 需求记录 (性能重构与 UI 细节打磨)：**
1. **工业级虚拟化架构升级**：彻底废除 DB 模式下的全量装载模型 `QStandardItemModel`，重构为基于 `QAbstractTableModel` 的虚拟化模型 `FerrexVirtualDbModel`。通过 `canFetchMore/fetchMore` 机制实现百万级数据的秒开与极低内存占用，彻底根治了由于数据量大导致的 UI 假死问题。
2. **异步化数据流水线**：
   - 实现了目录加载、全局搜索、全量分类加载的全面异步化（`QtConcurrent`），确保主线程 I/O 零阻塞。
   - 引入元数据预读（Prefetch）机制，在后台静默填充缓存，消除滚动时的磁盘访问抖动。
   - 将统计计数逻辑（ScanStats）下沉至 SQL 层执行 `COUNT(*)` 聚合，大幅提升统计效率。
3. **评级交互视觉优化（用户强制要求）**：
   - **动态胶囊收缩**：在非交互状态（未选中且未悬停）下，若项目已有评级，则仅绘制实心星级并隐藏所有空星位，彩色胶囊背景应随之动态收缩，禁绝右侧留白。
   - **交互恢复**：一旦进入交互状态（选中或悬停）或项目未评级（0星），恢复显示完整的 5 星占位位和“禁止”图标，以支持用户修改评分。
   - **中性色锁定**：物理锁定了评级辅助图标（禁止符、空星）的颜色为中性灰色 `#CCCCCC`，严禁出现误导性的红色高亮。
4. **物理 Bug 修复**：
   - **磁盘根目录修复**：修复了磁盘分区（如 `H:\`）在列表中名称显示为空的 Bug，现在能够正确显示盘符。
   - **架构对齐**：将 `ScanDialog` 的虚拟化加载理念完整迁移至主界面，实现了 FERREX 架构的逻辑归一。
