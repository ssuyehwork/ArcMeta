---
**2026-06-18 需求记录 (纠正逻辑偏差与架构归一)：**
1. **工业级虚拟化架构升级**：彻底废除 DB 模式下的全量装载模型 `QStandardItemModel`，重构为基于 `QAbstractTableModel` 的虚拟化模型 `FerrexVirtualDbModel`。实现百万级数据的瞬时加载与极低内存占用。
2. **评级交互逻辑修正（纠正脑补）**：
   - **占位符恢复**：已录入项目（In Database）即便未选中且未打分，也应始终显示评级占位区（禁止符 + 5颗空星），确保交互反馈的连贯性。
   - **禁止图标逻辑**：纠正此前的隐藏逻辑。禁止图标（Ban Icon）现在与 5 星位同步显示——在交互状态（选中/悬停）或未打分（0星）时强制显示；仅在项目已有打分且处于非交互状态时，为了视觉简洁才执行“隐藏禁止符与空星”的收缩优化。
   - **胶囊背景同步**：彩色胶囊背景的宽度现在严格根据当前显示的图标数量（1图标或 6图标）动态调整，禁绝任何形式的右侧留白或左侧偏移。
3. **磁盘根目录显示修复**：修复驱动器（如 `C:\`）在列表视图中名称为空的 Bug，现在强制显示盘符。
4. **头文件依赖加固**：物理补全了 `UiHelper.h` 中缺失的 `QDirIterator` 等头文件，确保工业级高效判定函数的编译鲁棒性。
